# 验证记录与行为场景

## 第五次更新：基础审美与通用排版部件（2026-09-10）

- 新增规则单独标记为本轮工程建议，不追记为课程内容。首次绘图加载 visual-design；美化/布局返工按需加载。可定位的审美缺陷与运行错误共用原有一次返工机会，未增加循环次数。
- 新增角色化样式、平坦 RGBA 背景黑白文字选择、主轴关联右色条、上方外置图例和只读视觉几何检查。没有图型模板库、新 scripts、配置写入器或统计运算接口。非负强度情境只改变未确认时的临时选色，明确候选及已确认项目选择优先。
- Windows / Python 3.12 / Matplotlib 3.11.1：48 项测试，47 通过、1 跳过（Windows 符号链接权限不足）、0 失败。覆盖原有回归，以及色条在主轴移动/画布缩放/DPI 改变后的物理间距与对齐、矩阵/norm/cmap/范围不变、重复和错误对象拒绝、图例顺序、色彩透明度叠加、项目配置优先级和告警不改布局。
- 维护者从两张目标的保存 CSV 编写隔离绘图代码，不导入旧 renderer；另绘制一张明确标为合成数据的双系列折线。Q2 矩阵与 artist 一致；Q3 沿既定口径按各组 converged 且 reachable 的记录计频率，列和为 1；读取的三个 CSV 哈希前后不变。没有修改比赛 code、数据、正式图或项目配色配置。
- 三张 PNG 已查看；Q2 初次 97.5% 自动标注被轴边裁切，使用一次返工将文字锚定于既有等高线内部，未改曲线。Q3 和折线的第二批 PNG 与已检查第一批哈希相同。
- 三张最终 PDF 经 Poppler 独立渲染并查看：Q3 和折线未见明显裁切/错位；**Q2 全矢量网格仍有查看器可见的面元拼接细缝，PNG 无此问题。一次返工额度已用完，没有第三次出图；该 PDF 未通过正式成图检查。** 导出 diagnostics 为空不能覆盖这一视觉问题，不宣称所有样张合格。
- 当前仅为维护者接口集成验证，**未执行 Terra high 或其他独立模型的盲测**，不证明低成本模型首次绘图审美达标，也没有人工验收。项目专用测试任务留在项目临时工作区，不随 skill 发布；不自动唤醒其他对话或切换其模型。

## 第四次定向更新：规则落实为模板接口（2026-09-09）

- 新增 `prepare_figure`：固定中英文字体和 bold、中文同色细矢量描边、所选主轴黑色实线、指定 Y 标签和右侧竖直色条名称水平化。重复调用不叠加描边；已有自定义文字效果保留并提示。axis-off 面板不被强行加坐标轴。不会修改网格、数值编码或画布尺寸。
- 新增 `check_label_spacing`，并接入 `save_figure` 的显式 data_axes/colorbars 参数。已验证能识别“仍在画布内、但放在色条左侧”的旧问题，返回间距和方向提示，不自动移动布局。
- 自动测试采用隔离合成数据；40 项中 39 通过、1 跳过（Windows 符号链接权限不足）、0 失败。覆盖中文描边在 SVG 中实际为同色 0.25 pt 矢量路径、幂等性、原有效果保留、轴线可见性、网格/矩阵/norm/色图/范围保真、越界对象拒绝、间距告警、双面板色条关联，以及旧版回归。
- 另外仅在隔离目录加载两张用户指定图的已保存数据副本和出图函数副本。没有运行模型入口；原始源码、数据和六个 PDF/PNG/SVG 图文件的哈希均保持。对比实际 artist 的矩阵、曲线顶点、数值范围、色图采样、数值注释、基准框及其位置，修改前后一致。
- 两张图的隔离新版已查看 PNG 与独立渲染的 PDF：中文视觉加粗，主轴黑色清晰，Y 标签和色条名称水平可读；MC 长 Y 标签仅增加换行，原词语不变。画布加宽至 8.5 英寸，主图区原有宽度（4.68 / 4.615 英寸）、高度及数据比例保持；采用原 fixed-Axes 布局的定点留白，不调用无效的 tight_layout。这个宽度只用于本次样张，不是 skill 全局默认。
- 原图色条位置和间距告警在新版中消失；PDF 色条拼接细缝按已有不透明色条规则修复一轮。保留原图栅格主图层，导出仍明确提示混合矢量/栅格，未宣称纯矢量或改变原图栅格化策略。
- 本次为 Agent 检查，不代表论文手认可版面。两张比赛原图及项目出图代码未覆盖；只验证模板调用效果。没有部署/替换项目 code 下的共用模块，后续使用仍须按参考接入调用。既有早期记录为历史，不代表新接口仍缺失。

## 第三次定向更新：色条间距、水平标签与加粗（2026-09-08）

- 实际查看用户试跑的 Q2 概率热力图与 Q3 蒙特卡洛分布图 PNG，并核对生成源码：两个色条均为 labelpad=8、label_position=left；目标样式沿用浅灰轴线。画布内文字检查没有验证主图与色条名称之间的留白。
- 已将外置色条名称优先水平、标签间距检查、字体视觉加粗、黑色实线轴和最小线宽写入运行规则。模板新增黑色轴/刻度默认及拒绝弱化字重、颜色、线宽的参数检查。
- 回归 33 项：32 通过、1 跳过（Windows 符号链接权限不足）。新测试实际检查轴脊黑色/线宽及标签字重，不仅匹配规则文字。
- 本轮没有覆盖或重绘用户两张图；新布局与宋体同色描边属于调用方需要执行的规则，尚未做新图视觉验证，不宣称用户反馈已在成图中解决。当前模板仍不会自动摆放任意旧图的色条或为宋体补描边。下一轮应在隔离副本验证两张图的布局，之后再由用户决定替换。

## 第二版：论文手风格与配色试选（2026-09-08）

本轮只更新 skill 源文件，不部署、不读取比赛数据。保留首次绘图/审查返工两种模式，新增按需配色参考；模板仍为可复制到项目的单个 Python 文件，无 R、Pillow、配置写入器或新日志系统。

### 自动验证

- Windows、Python 3.12.14、Matplotlib 3.11.1；安装字体为 SimSun 和 Times New Roman。
- 32 项测试：31 通过、1 跳过、0 失败。跳过仍为 Windows 权限不允许的真实目录符号链接测试；不可宣称该分支已验证。
- skill-creator 官方元数据检查返回 `Skill is valid!`；入口及全部参考本地链接有效。
- 新增/更新实测：固定字体且拒绝替换、SVG 中英实际字形来源、缺普通/数学字形拒绝输出、非空轴标题与总标题拒绝且不静默删除、各类型配色独立读取、部分确认不自动确认其他类型、试色不改项目配置、损坏配置不重置。
- 四次分类色替换保持折线数据、布局位置、轴范围、字号字重、线型、标记、画布一致；连续色替换保持矩阵、同一 norm 对象、范围和中点，色条与主图同步，测试 SVG 不含 image 层。
- 复制进项目后禁用模拟 skill 目录，由新 Python 子进程读取项目已保存的 Lancet 选择并成功导出。没有项目依赖 skill 安装路径。
- 原有数据/非目标保护、路径边界、覆盖前冲突检查、局部样式恢复、一次裁切修复等回归用例保留通过。

### 实际成图检查

`tests/render_examples.py` 生成 15 组 PDF/SVG/PNG 合成样张：基础折线、4 个分类试色、柱图、裁切负例及一次修复、中英混排、3 个顺序色图及 3 个发散色图。PNG 逐张检查；最终未改样张通过与已看版本的图像文件哈希比对确认相同，改动样张重新查看。正式 PDF 选取折线、柱图、裁切前后、中英混排、cividis 和 RdBu_r 共 7 张，用 Poppler 独立渲染并检查。

- 正常样张无图内标题，轴标签、图例和负号可读；中英混排用固定字体。
- 03_clipped 故意将已给定来源注释放在画布外，确实裁切并报警；04_repaired 仅移回图内空白处，告警消失。注释只用于裁切测试，不要求真实图新增来源注释。
- 分类候选的曲线形态、布局与线型相同，仅颜色改变。
- 连续色图的数值范围、中点和刻度保持；处理色条自动光栅化及 PDF 拼接细缝后，所检查正式 PDF 色条连续可读。相关处理仅用于合适的不透明色条，未设为对所有图强制后处理。
- 最终样张中仅故意裁切负例返回警告。SVG 字形与图像层做结构测试，未用独立 SVG 查看器逐张复核。只检查两个连续色板对应的正式 PDF，其余连续候选正式 PDF 未逐张独立查看。

这些结果是 Agent 检查，**不是人工验收**；没有选择真实项目默认方案。字号和字重仍需论文手按最终版面判断。本机宋体使用常规字重，英文使用 Times New Roman 粗体，不能宣称宋体存在真实粗体字形。

### 流程走查与新增边界

“只审查不写文件”“仅换配色不顺便改数据/布局”“试用不更新默认”“人明确确认后仅保存相应类型”“新默认不自动重绘旧图”“一轮自动修复与人工试色次数分开”已做指令一致性走查，尚未独立 Agent 行为实测。

模板不是任意源码自动修复器：显式 artist 字体仍须在源码修正；用普通注释伪装的标题须语义审查；读取配置不证明真的得到人工授权。模板的字形守卫覆盖本次实际测试的普通标签和 mathtext 缺字情形，不保证所有公式、缓存或外部后端均被自动识别。保留视觉核验与一轮后交人工的边界。

## 第一版历史记录（以下不是当前字体与标题规则）

验证日期：2026-09-08。未使用比赛数据，未部署到比赛项目，未触发训练或模型重跑。下述结果区分代码运行、视觉检查和指令走查，不混称为“全部自动验证”。

## 已执行的自动验证

- 环境：Windows，Python 3.12.14，Matplotlib 3.11.1。中文样张选择 Microsoft YaHei。
- `python -m unittest discover -s tests -v`：23 项，22 项通过、1 项跳过、0 项失败。
- 跳过项：真实目录符号链接越界测试。该 Windows 运行权限不能创建符号链接（WinError 1314）；不能声称该分支已实测。普通父目录逃逸、同名前缀兄弟目录及 `.agents`/`.git` 输出拒绝均已实测。
- skill-creator 官方 `quick_validate.py`：返回 `Skill is valid!`。PyYAML 6.0.3 仅装入临时验证目录，不是本 skill 的运行依赖。
- 运行引用链接检查：入口与 references 的本地链接有效，未指向历史资料或测试目录。

自动测试覆盖：局部配置及异常后恢复、嵌套上下文、拒绝改变 backend、缺字体、缺中文字形、负号配置、默认 PDF/PNG、SVG 不含图像层、原绘图点保持不变、输出越界拒绝、整批输出冲突检查、显式覆盖与保留 before、一次裁切修复、保留已有布局、不用 tight 裁框掩盖溢出、栅格层提示、参数错误，以及图型更换时类别数值保持和批量非目标保护。

独立项目复现测试将模块从模拟 skill 复制进项目，随后把 skill 安装目录改名，使用新 Python 子进程从另一个工作目录运行项目代码，成功生成图片。测试中没有引用原 skill 模板路径；这验证了项目副本的运行独立性，不代表已经在 Codex 界面安装并触发了此 skill。

## 已执行的视觉检查

运行 `render_examples.py` 生成五组 PDF/SVG/PNG 合成样张。逐张查看 PNG，并用 Poppler 将五张正式 PDF 独立渲染为 PNG 后逐张查看：

| 样张 | 预期 | 实际观察 |
| --- | --- | --- |
| 01_line | 双系列趋势，颜色与线型区分 | PNG 与 PDF 标题、轴标签、图例及线条可读，无明显遮挡 |
| 02_bar | 类别比较，横向柱图零基线 | 类别完整、起点为零、字体及留白清楚 |
| 03_clipped | 故意令标题超出画布 | 实际发生裁切；导出返回画布溢出提示，是负面测试，不是合格样图 |
| 04_repaired | 仅调整标题位置与对齐 | 裁切消失、提示消失；数据与折线未改变 |
| 05_chinese | 中文标签及负数刻度 | 两种渲染中中文、负号正常，无方块或明显裁切 |

SVG 的结构由自动测试检查，未逐张通过独立 SVG 渲染器复核。视觉检查是本次 Agent 的实际观察，未记为用户人工确认。

## 行为场景走查（非独立 Agent 实测）

以下检查入口与参考文件是否给出了可执行、无冲突的处理规则；没有用字符串匹配测试冒充模型行为验证，也没有声称具备任意旧源码的自动正确修复能力。

| 场景请求 | 预期行为与规则位置 | 本轮状态 |
| --- | --- | --- |
| “只审查这张截图” | review-repair：只读并报告；缺数据时不猜值，不生成修改或文件 | 指令走查通过；尚未独立 Agent 实测 |
| “已保存结果画首次图” | first-plot：找来源、选型、接项目内模块、运行初检 | 指令走查；对应模板首次绘图已运行 |
| “原图型错配，修改它” | chart-guide + review-repair：已有依据下可换型，不改统计口径 | 指令走查；数值保持的换型代码测试通过 |
| “这批图全部统一，别动其他图” | review-repair：查共用调用方、局部覆盖，逐图检查 | 指令走查；模板批量非目标文件保护测试通过 |
| “出图主入口顺便会训练” | SKILL + 两模式流程：先找/分离只读入口；做不到则停止该部分 | 指令走查通过；未执行任何训练入口 |
| “第一轮改完还有遮挡” | SKILL：最多一轮自动修复，仍有问题交人工，不继续审美循环 | 指令走查；裁切样例执行一次定点修复，不证明模型必然遵守次数 |
| “人工已看完，说需要换布局” | SKILL + style-export：针对性修复，必要时 preserve 已选布局并说明 | 指令走查；保留 constrained 布局的调用测试通过 |

将来做独立行为验证时，可用这些场景和隔离合成项目作为任务输入；未获授权不自动派生其他任务或扩展到真实比赛工程。

## 已知边界

- 模板不解析任意项目的数据流程，不做语义认证，也不自动判断所有文本重叠。
- 普通标签的字体检测不能替代数学公式、外部 TeX 或特殊字体后端核验。
- 保存前会将所有格式先渲染到内存，避免渲染失败产生半套文件；实际磁盘写入失败仍可能留下部分输出，应报告并按已保留的 before 恢复，不承诺跨文件事务。
- 不应在同一目标路径同时运行多个写入者；写入前路径复核不是操作系统级沙箱或恶意竞态防护。
- 不同机器字体及 Matplotlib 版本会产生差异；本轮验证范围如上，不承诺像素级跨机一致。

## 复跑

使用已有环境从本仓库运行单元测试。视觉样张指定一个尚不存在的隔离输出目录；脚本不会覆盖已有样张。测试用临时项目，不要求与本次机器路径相同。

维护测试和本记录不部署到日常运行的 skill 副本。若修改模板或指令，应复跑对应代码测试并重新走查受影响的行为场景。
