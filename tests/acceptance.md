# 第一版验证记录与行为场景

验证日期：2026-09-08。未使用比赛数据，未部署到比赛项目，未触发训练或模型重跑。下述结果区分代码运行、视觉检查和指令走查，不混称为“全部自动验证”。

## 已执行的自动验证

- 环境：Windows，Python 3.12.14，Matplotlib 3.11.1。中文样张选择 Microsoft YaHei。
- `python -m unittest discover -s tests -v`：23 项，22 项通过、1 项跳过、0 项失败。
- 跳过项：真实目录符号链接越界测试。该 Windows 运行权限不能创建符号链接（WinError 1314）；不能声称该分支已实测。普通父目录逃逸、同名前缀兄弟目录及 `.agents`/`.git` 输出拒绝均已实测。
- skill-creator 官方 `quick_validate.py`：返回 `Skill is valid!`。PyYAML 6.0.3 仅装入临时验证目录，不是本 skill 的运行依赖。
- 运行引用链接检查：入口与 references 的本地链接有效，未指向历史资料或测试目录。

自动测试覆盖：局部配置及异常后恢复、嵌套上下文、拒绝改变 backend、缺字体、缺中文字形、负号配置、默认 PDF/PNG、SVG 不含图像层、原绘图点保持不变、输出越界拒绝、整批输出冲突检查、显式覆盖与保留 before、一次裁切修复、保留已有布局、不用 tight 裁框掩盖溢出、栅格层提示、参数错误，以及图型更换时类别数值保持和批量非目标保护。

独立项目复现测试将模块从模拟 skill 复制进项目，随后把 skill 安装目录改名，使用新 Python 子进程从另一个工作目录运行项目代码，成功生成图片。测试中没有引用原 skill 模板路径；这验证了项目副本的运行独立性，不代表已经在 Codex 界面安装并触发了此 skill。

## 已执行的视觉检查

运行 `render_examples.py` 生成五组 PDF/SVG/PNG 合成样张。逐张查看 PNG，并用 Poppler 将五张正式 PDF 独立渲染为 PNG 后逐张查看：

| 样张 | 预期 | 实际观察 |
| --- | --- | --- |
| 01_line | 双系列趋势，颜色与线型区分 | PNG 与 PDF 标题、轴标签、图例及线条可读，无明显遮挡 |
| 02_bar | 类别比较，横向柱图零基线 | 类别完整、起点为零、字体及留白清楚 |
| 03_clipped | 故意令标题超出画布 | 实际发生裁切；导出返回画布溢出提示，是负面测试，不是合格样图 |
| 04_repaired | 仅调整标题位置与对齐 | 裁切消失、提示消失；数据与折线未改变 |
| 05_chinese | 中文标签及负数刻度 | 两种渲染中中文、负号正常，无方块或明显裁切 |

SVG 的结构由自动测试检查，未逐张通过独立 SVG 渲染器复核。视觉检查是本次 Agent 的实际观察，未记为用户人工确认。

## 行为场景走查（非独立 Agent 实测）

以下检查入口与参考文件是否给出了可执行、无冲突的处理规则；没有用字符串匹配测试冒充模型行为验证，也没有声称具备任意旧源码的自动正确修复能力。

| 场景请求 | 预期行为与规则位置 | 本轮状态 |
| --- | --- | --- |
| “只审查这张截图” | review-repair：只读并报告；缺数据时不猜值，不生成修改或文件 | 指令走查通过；尚未独立 Agent 实测 |
| “已保存结果画首次图” | first-plot：找来源、选型、接项目内模块、运行初检 | 指令走查；对应模板首次绘图已运行 |
| “原图型错配，修改它” | chart-guide + review-repair：已有依据下可换型，不改统计口径 | 指令走查；数值保持的换型代码测试通过 |
| “这批图全部统一，别动其他图” | review-repair：查共用调用方、局部覆盖，逐图检查 | 指令走查；模板批量非目标文件保护测试通过 |
| “出图主入口顺便会训练” | SKILL + 两模式流程：先找/分离只读入口；做不到则停止该部分 | 指令走查通过；未执行任何训练入口 |
| “第一轮改完还有遮挡” | SKILL：最多一轮自动修复，仍有问题交人工，不继续审美循环 | 指令走查；裁切样例执行一次定点修复，不证明模型必然遵守次数 |
| “人工已看完，说需要换布局” | SKILL + style-export：针对性修复，必要时 preserve 已选布局并说明 | 指令走查；保留 constrained 布局的调用测试通过 |

将来做独立行为验证时，可用这些场景和隔离合成项目作为任务输入；未获授权不自动派生其他任务或扩展到真实比赛工程。

## 已知边界

- 模板不解析任意项目的数据流程，不做语义认证，也不自动判断所有文本重叠。
- 普通标签的字体检测不能替代数学公式、外部 TeX 或特殊字体后端核验。
- 保存前会将所有格式先渲染到内存，避免渲染失败产生半套文件；实际磁盘写入失败仍可能留下部分输出，应报告并按已保留的 before 恢复，不承诺跨文件事务。
- 不应在同一目标路径同时运行多个写入者；写入前路径复核不是操作系统级沙箱或恶意竞态防护。
- 不同机器字体及 Matplotlib 版本会产生差异；本轮验证范围如上，不承诺像素级跨机一致。

## 复跑

使用已有环境从本仓库运行单元测试。视觉样张指定一个尚不存在的隔离输出目录；脚本不会覆盖已有样张。测试用临时项目，不要求与本次机器路径相同。

维护测试和本记录不部署到日常运行的 skill 副本。若修改模板或指令，应复跑对应代码测试并重新走查受影响的行为场景。
